type typeA.
type typeB.
free ch: channel [private].

event frititatiGay(typeA).
query v: typeB; event(frititatiGay(v)).

let pA() = 
new vA: typeA;
  out(ch, vA);
  in(ch, variable: typeA);
  event frititatiGay(variable)
  .

process
   pA()