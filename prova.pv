type typeA.
type typeB.
free ch: channel [private].

event reach().
query v: typeA; event(reach()).

let pA() = 
new vA: typeA;
  out(ch, vA);
  event reach()
  in(ch, variable: typeA);
  
  .

process
   pA()