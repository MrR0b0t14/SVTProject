type typeA.
type typeB.
free ch: channel [private].

event reach(typeA).
query v: typeA; event(reach(v)).

let pA() = 
new vA: typeA;
  out(ch, vA);
  in(ch, variable: typeA);
  event reach(variable)
  .

process
   pA()